import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MusterPage } from './muster';

@NgModule({
  declarations: [
    MusterPage,
  ],
  imports: [
    IonicPageModule.forChild(MusterPage),
  ],
})
export class MusterPageModule {}
